# Get the hash of the package from the DESCRIPTION file

Get the hash of the package from the DESCRIPTION file

## Usage

``` r
read_hash_from_description(package_dir)
```

## Arguments

- package_dir:

  Path to package
