# Make error messages noticeable

Since litr error messages are amid a lot of output from knitting, we'd
like the litr ones to be eye-catching.

## Usage

``` r
make_noticeable(msg)
```

## Arguments

- msg:

  Error message
