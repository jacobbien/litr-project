# Return everything `setup()` changed to its original state

Return everything [`setup()`](setup.md) changed to its original state

## Usage

``` r
restore_state(original_state)
```

## Arguments

- original_state:

  As returned by [`setup()`](setup.md)
