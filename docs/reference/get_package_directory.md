# Get package directory

Get package directory

## Usage

``` r
get_package_directory(package_parent_dir, package_name, input)
```

## Arguments

- package_parent_dir:

  The directory of where the package should go (relative to the input
  directory)

- package_name:

  The name of the package

- input:

  The file name of the input
