# Write the hash of the package to the DESCRIPTION file

Write the hash of the package to the DESCRIPTION file

## Usage

``` r
write_hash_to_description(package_dir)
```

## Arguments

- package_dir:

  Path to package
