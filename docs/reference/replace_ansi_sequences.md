# Replace ANSI escape sequences with their HTML equivalents

Finds ANSI escape sequences and replaces them with HTML tags using the
`fansi` package

## Usage

``` r
replace_ansi_sequences(html_files)
```

## Arguments

- html_files:

  Character vector of file names of html files that were created from
  Rmd files
